/**
 * @description export related interfaces by model. Add relavant model permission.
 * @since 1.0.0
 * @returns restful interfaces by 
 */
module.exports = {
  message: {
    model: require("../../schemas/message"),
  }
};